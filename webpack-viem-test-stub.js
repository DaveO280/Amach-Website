/**
 * Stub for viem's actions/test/dropTransaction.
 * Replaced at build time so test-only code is not bundled.
 */
function dropTransaction() {
  return Promise.resolve(undefined);
}

module.exports = { dropTransaction };
module.exports.default = dropTransaction;
